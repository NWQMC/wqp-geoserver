@Library(value='PipelineLibs@2.y', changelog=false) _

pipeline {
    agent {
        node {
            label 'project:any'
        }
    }
    stages {
        stage('build artifact') {
            steps {
                serviceDeployPipeline()
            }
        }
    }
}
